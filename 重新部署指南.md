# 重新部署到 Cloudflare Pages 完整指南

## 🎯 方案 1：手動觸發新部署（推薦）

### 步驟 1：在 Cloudflare Dashboard 中觸發部署

1. **登入 Cloudflare Dashboard**
   - 前往：https://dash.cloudflare.com/
   - 登入您的帳戶

2. **進入 Pages 項目**
   - 點擊左側菜單 **"Workers & Pages"**
   - 點擊 **"Pages"** 標籤
   - 找到項目 **"global-constellation"** 並點擊

3. **手動觸發部署**
   - 點擊 **"Deployments"** 標籤
   - 點擊右上角的 **"Create deployment"** 按鈕
   - 選擇分支：**"main"**
   - 點擊 **"Deploy"**

### 步驟 2：等待構建完成

- 構建通常需要 3-5 分鐘
- 可以在 "Deployments" 頁面查看構建進度
- 綠色 ✅ 表示成功，紅色 ❌ 表示失敗

---

## 🔄 方案 2：重新設置 Cloudflare Pages 項目

如果構建持續失敗，可以重新設置項目：

### 步驟 1：刪除現有項目（可選）

1. 在 Cloudflare Pages 項目頁面
2. 點擊 **"Settings"** 標籤
3. 滾動到底部
4. 點擊 **"Delete project"**
5. 確認刪除

### 步驟 2：創建新項目

1. **創建新項目**
   - 在 Pages 列表頁面，點擊 **"Create a project"**
   - 選擇 **"Connect to Git"**

2. **連接 GitHub**
   - 選擇 **"GitHub"**
   - 授權 Cloudflare 訪問（如果尚未授權）
   - 選擇倉庫：**"HKCalvinYau/GLOBAL-CONSTELLATION"**
   - 點擊 **"Begin setup"**

3. **配置構建設置**
   - **Project name**: `global-constellation` 或 `amar-constellation`
   - **Framework preset**: 選擇 **"Astro"**
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
   - **Root directory**: `/`（留空）
   - **Node.js version**: 選擇 `20` 或 `18`

4. **設置環境變數**（重要！）
   - 點擊 **"Environment variables"** 或 **"Variables"**
   - 添加以下變數（Production 環境）：
     ```
     KEYSTATIC_GITHUB_OWNER=HKCalvinYau
     KEYSTATIC_GITHUB_REPO=GLOBAL-CONSTELLATION
     KEYSTATIC_GITHUB_TOKEN=您的GitHub_Token
     ```
   - 點擊 **"Save"**

5. **保存並部署**
   - 點擊 **"Save and Deploy"**
   - 等待構建完成

---

## 🛠️ 方案 3：本地構建測試後再部署

### 步驟 1：本地測試構建

```bash
# 1. 確保在項目目錄
cd "/Users/calvinyau/Documents/Calvin Project/GLOBAL CONSELL"

# 2. 安裝依賴（如果需要）
npm install

# 3. 測試構建
npm run build
```

### 步驟 2：如果本地構建成功

```bash
# 推送到 GitHub（觸發自動部署）
git add -A
git commit -m "測試構建"
git push origin main
```

### 步驟 3：如果本地構建失敗

查看錯誤訊息，修復問題後再推送。

---

## 🔍 方案 4：檢查並修復構建錯誤

### 步驟 1：查看構建日誌

1. 在 Cloudflare Pages 項目頁面
2. 點擊 **"Deployments"** 標籤
3. 點擊最近失敗的部署
4. 點擊 **"View details"**
5. 查看 **"Build logs"** 或 **"Deployment logs"**
6. 複製錯誤訊息

### 步驟 2：根據錯誤修復

常見錯誤及解決方案：

#### 錯誤 1：`Cannot bundle Node.js built-in "node:path"`
**解決方案**：確保已設置 GitHub 環境變數，使用 GitHub 存儲

#### 錯誤 2：`Module not found`
**解決方案**：檢查 `package.json` 中的依賴是否完整

#### 錯誤 3：`Build command failed`
**解決方案**：檢查構建命令是否正確：`npm run build`

---

## 📋 快速檢查清單

在重新部署前，確認：

- [ ] GitHub 倉庫已更新（最新代碼已推送）
- [ ] Cloudflare Pages 項目已連接到正確的 GitHub 倉庫
- [ ] 環境變數已設置（`KEYSTATIC_GITHUB_OWNER`、`KEYSTATIC_GITHUB_REPO`、`KEYSTATIC_GITHUB_TOKEN`）
- [ ] 構建命令正確：`npm run build`
- [ ] 輸出目錄正確：`dist`
- [ ] Node.js 版本設置為 18 或 20

---

## 🚀 推薦流程

1. **先嘗試方案 1**（手動觸發部署）- 最簡單
2. **如果失敗，查看構建日誌**（方案 4）
3. **根據錯誤修復代碼**
4. **重新推送代碼到 GitHub**
5. **如果持續失敗，考慮方案 2**（重新設置項目）

---

## 💡 提示

- **自動部署**：設置完成後，每次推送到 `main` 分支都會自動觸發部署
- **構建時間**：首次構建可能需要 5-10 分鐘
- **環境變數**：必須設置 GitHub Token 才能使用 Keystatic
- **構建日誌**：如果構建失敗，一定要查看構建日誌找出原因

---

## 🔗 相關鏈接

- Cloudflare Pages Dashboard: https://dash.cloudflare.com/
- GitHub 倉庫: https://github.com/HKCalvinYau/GLOBAL-CONSTELLATION
- 構建日誌：在 Cloudflare Pages 項目 > Deployments > 點擊部署 > View details

